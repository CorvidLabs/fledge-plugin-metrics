# fledge.toml — project task definitions
[tasks]
fmt = "cargo fmt --check"
lint = "cargo clippy -- -D warnings"
test = "cargo test"
build = "cargo build --release"
smoke = "./target/release/fledge-metrics --help >/dev/null && ./target/release/fledge-metrics --json >/dev/null"

[lanes.verify]
description = "Format, lint, test, build, and smoke-test the metrics plugin"
steps = ["fmt", "lint", "test", "build", "smoke"]
